<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle or siteData.name }}</title>
    <link rel='stylesheet' href='{{ 'src/assets/css/styles.css' | url }}' />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>